(ns simba.constants)

(def allowed-symbols
   #{ * *' +' - -' + < <= = == > >= apply assoc assoc-in boolean boolean-array booleans butlast cast cat char char-array char? class chars class? coll? commute comp comparator compare complement concat conj cons constantly contains? count cycle dec dec' decimal? disj descendants dissoc distinct distinct? double double-array doubles drop drop-last drop-while empty empty? even? every-pred every? extend false? filter filterv find first find-keyword flatten float float? get get-in group-by hash identical? identity inc inc' int integer? interleave interpose into into-array isa? iterate juxt keep keep-indexed key keys keyword keyword? last list list* list? long map map-indexed map? map-entry? mapcat mapv max max-key merge merge-with min min-key mod neg? newline next nil? not not-any? not-empty not-every? not= nth nthnext nthrest num number? numerator odd? partial partition partition-all partition-by peek pmap pop pos? quot rand rand-int rand-nth random-sample range ratio? rational? rationalize re-find re-groups re-matcher re-matches re-pattern re-seq record? reduce reduce-kv reduced reduced? rem repeat repeatedly replace replicate resolve rest reverse reversible? rseq rsubseq satisfies? second select-keys seq seq? sequence seque sequential? set? short some some? sort sort-by sorted-map sorted-map-by sorted-set sorted-set-by sorted? spit split-at split-with str string? subs subseq subvec symbol symbol? take take-last take-nth take-while to-array transduce tree-seq true? update update-in val vals vec vector vector-of vector? zero? zipmap })